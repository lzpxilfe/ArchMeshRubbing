# Supported source/runtime environment. Keep the human-facing install command
# stable while resolving exactly the same versions used by frozen builds and
# the offline self-test.
-r requirements/runtime-py312.lock
